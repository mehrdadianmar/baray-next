# راهنمای نهایی اتصال وردپرس و استقرار روی هاست فعلی

## چه چیزهایی در وردپرس مدیریت شوند؟

### اکنون در وردپرس

- اخبار، مقالات و رویدادها
- عنوان، خلاصه، متن، نویسنده، تاریخ، دسته‌بندی و تصویر شاخص
- پیش‌نویس، زمان‌بندی انتشار و وضعیت انتشار

### پیشنهاد مرحله بعد

- راهنماهای مرکز دانش
- مطالعات موردی و پروژه‌ها
- پرسش‌های متداول محصولات، در صورت تغییر مکرر
- آگهی‌ها، دانلودها و رویدادهای شرکتی

### بهتر است در کد بمانند

- هدر، فوتر، منو و سیستم طراحی
- ساختار صفحه اصلی و ترتیب بخش‌ها
- منطق SEO، Schema و Sitemap
- قابلیت‌های قطعی محصولات و متن‌های حساس قراردادی
- تنظیمات امنیتی و اتصال سرویس‌ها

اطلاعات تماس بهتر است بعداً از یک «تنظیمات عمومی سایت» کنترل‌شده دریافت شود، نه از متن آزاد نوشته‌ها.

## تنظیم وردپرس

1. پیوندهای یکتا روی «نام نوشته» قرار گیرد.
2. REST API در مسیر `/wp-json/wp/v2` فعال بماند.
3. هر نوشته خلاصه اختصاصی، تصویر شاخص و دسته‌بندی دقیق داشته باشد.
4. نامک نوشته ترجیحاً کوتاه، انگلیسی و پایدار باشد.
5. افزونه‌های امنیتی نباید درخواست خواندن نوشته‌های منتشرشده را مسدود کنند.
6. پیش‌نویس‌ها از API عمومی خوانده نمی‌شوند و در سایت جدید نمایش داده نخواهند شد.

برای دریافت محتوای عمومی نیازی به نام کاربری، رمز یا Application Password نیست.

## استقرار مستقل از نوع هاست

فرآیند ساخت به Node.js نیاز دارد، اما خروجی عمومی فقط HTML، CSS، JavaScript و تصویر است. در نتیجه خروجی `out` روی هاست اشتراکی PHP، cPanel، Apache، Nginx یا فضای استاتیک قابل استفاده است.

```bash
npm ci
cp .env.example .env.local
npm run build:wordpress
```

سپس محتویات `out` داخل ریشه دامنه، معمولاً `public_html`، بارگذاری می‌شود.

## انتقال وردپرس پس از تأیید

در دوره آزمایش، API می‌تواند از `www.baray.ir` خوانده شود. هنگام جایگزینی سایت اصلی، بهتر است پنل وردپرس به `cms.baray.ir` منتقل شود و مقدار زیر تغییر کند:

```env
WORDPRESS_API_URL=https://cms.baray.ir/wp-json/wp/v2
```

پوشه `wp-content/uploads` و آدرس رسانه‌ها باید در انتقال حفظ شوند. اسکریپت همگام‌سازی تصاویر شاخص را محلی می‌کند تا فرانت نهایی به آدرس قدیمی تصویر وابسته نباشد.

## نکات حیاتی SEO

- نامک نوشته‌های دارای ورودی گوگل تا حد امکان حفظ شود.
- برای هر URL تغییرکرده Redirect دائمی 301 تک‌به‌تک تعریف شود.
- نسخه آزمایشی عمومی `noindex` باشد.
- قبل از انتقال، فهرست URLهای Search Console ذخیره شود.
- بعد از انتقال، Sitemap ثبت و خطاهای 404 پایش شوند.

## محدودیت صادقانه

روی هاست کاملاً استاتیک، انتشار خبر بدون اجرای Build ممکن نیست. برای انتشار خودکار باید هاست Node.js، Cron دارای Node یا یک سرویس Build/Webhook در دسترس باشد. این محدودیت معماری استاتیک است و نباید با دریافت خبر در مرورگر دور زده شود، زیرا آن روش کیفیت SEO و پایداری صفحه را کاهش می‌دهد.
