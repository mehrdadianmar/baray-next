import Link from "next/link";import type {Article} from "@/lib/articles";export default function NewsCard({a}: {a:Article}){return <article className="newsCard"><Link className="newsImage" href={`/news/${a.slug}`}><img src={a.image} width="1200" height="675" loading="lazy" alt={`تصویر مقاله ${a.title}`}/><span>{a.category}</span></Link><div className="newsBody"><div className="meta"><time dateTime={a.publishedAt}>{a.date}</time><i/><span>{a.source}</span></div><h2><Link href={`/news/${a.slug}`}>{a.title}</Link></h2><p>{a.excerpt}</p><Link className="textLink" href={`/news/${a.slug}`} aria-label={`مطالعه ${a.title}`}>مطالعه مقاله ←</Link></div></article>}
