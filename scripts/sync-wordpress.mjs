import fs from "node:fs/promises";
import path from "node:path";

const api=(process.env.WORDPRESS_API_URL||"https://www.baray.ir/wp-json/wp/v2").replace(/\/$/,"");
const target=path.resolve(process.env.WP_SNAPSHOT_PATH||"content/wordpress-posts.json");
const mediaDir=path.resolve(process.env.WP_MEDIA_DIR||"public/wp-media");
const strict=process.argv.includes("--strict");
const fixture=process.env.WORDPRESS_FIXTURE_PATH;
const limit=Number(process.env.WORDPRESS_POST_LIMIT||100);
const decodeSlug=(slug="")=>{try{return decodeURIComponent(slug)}catch{return slug}};
const fallbackImages=["/news/legal-ai.svg","/news/governance.svg","/news/integration.svg","/news/justice.svg","/news/data-migration.svg","/news/software-selection.svg"];
const semanticImage=(title="",index=0)=>{const value=title.toLowerCase();if(value.includes("هوش تجاری")||value.includes("business intelligence"))return "/news/business-intelligence.svg";if(value.includes("xbrl"))return "/news/xbrl-reporting.svg";if(value.includes("erp")||value.includes("برنامه ریزی منابع"))return "/news/erp-flow.svg";if(value.includes("اتوماسیون اداری"))return "/news/office-automation.svg";if(value.includes("قرارداد")&&value.includes("ریسک"))return "/news/contract-risk.svg";if(value.includes("هوش مصنوعی")||value.includes("حقوقی"))return "/news/legal-ai.svg";return fallbackImages[index%fallbackImages.length]};
const safeStem=(value="image")=>value.replace(/%/g,"").replace(/[^a-z0-9_-]/gi,"-").replace(/-+/g,"-").slice(0,140)||"image";
const strip=(html="")=>html.replace(/<script[\s\S]*?<\/script>/gi,"").replace(/<style[\s\S]*?<\/style>/gi,"").replace(/<[^>]+>/g," ").replace(/&nbsp;/g," ").replace(/&amp;/g,"&").replace(/&#8211;|&#8212;/g,"—").replace(/&#8230;/g,"…").replace(/\s+/g," ").trim();
const fetchJson=async(url)=>{const response=await fetch(url,{headers:{Accept:"application/json","User-Agent":"Baray-Content-Sync/1.0"},signal:AbortSignal.timeout(20000)});if(!response.ok)throw new Error(`${response.status} ${response.statusText}`);return response.json()};
const extension=(url,type="")=>{const ext=path.extname(new URL(url).pathname).toLowerCase();if(/^\.(jpe?g|png|webp|gif|svg)$/.test(ext))return ext;if(type.includes("png"))return ".png";if(type.includes("webp"))return ".webp";return ".jpg"};
const downloadImage=async(url,stem,fallback)=>{if(!url)return fallback;try{const ext=extension(url);const name=`${safeStem(stem)}${ext}`;await fs.mkdir(mediaDir,{recursive:true});if(url.startsWith("file:")){await fs.copyFile(new URL(url),path.join(mediaDir,name));return `/wp-media/${name}`}const response=await fetch(url,{headers:{"User-Agent":"Baray-Content-Sync/1.0"},signal:AbortSignal.timeout(15000)});if(!response.ok)throw new Error(String(response.status));await fs.writeFile(path.join(mediaDir,name),Buffer.from(await response.arrayBuffer()));return `/wp-media/${name}`}catch(error){console.warn(`Image fallback for ${stem}: ${error.message}`);return fallback}};
const localizeContentImages=async(html="",slug,fallback)=>{const urls=[...new Set([...html.matchAll(/<img\b[^>]*\bsrc=["']([^"']+)["']/gi)].map(match=>match[1]).filter(url=>/^(https?:|file:)/.test(url)))].slice(0,12);const replacements=await Promise.all(urls.map((url,index)=>downloadImage(url,`${slug}-content-${index+1}`,fallback)));return urls.reduce((content,url,index)=>content.split(url).join(replacements[index]),html)};
const readPage=async(page)=>fixture?(page===1?JSON.parse(await fs.readFile(fixture,"utf8")):[]):fetchJson(`${api}/posts?per_page=100&page=${page}&status=publish&_embed=1`);

try{
  const posts=[];for(let page=1;posts.length<limit;page++){let batch;try{batch=await readPage(page)}catch(error){if(page>1&&String(error.message).startsWith("400"))break;throw error}posts.push(...batch);if(batch.length<100)break}
  const result=[];for(const [index,post] of posts.slice(0,limit).entries()){const media=post._embedded?.["wp:featuredmedia"]?.[0];const terms=post._embedded?.["wp:term"]?.flat?.()||[];const author=post._embedded?.author?.[0]?.name||"تحریریه رایان بارای";const title=strip(post.title?.rendered);const excerpt=strip(post.excerpt?.rendered)||strip(post.content?.rendered).slice(0,180);const fallback=semanticImage(title,index);const preferEditorial=/هوش تجاری|xbrl|erp|برنامه ریزی منابع|اتوماسیون اداری/i.test(title);result.push({slug:decodeSlug(post.slug),title,excerpt,category:terms.find(x=>x.taxonomy==="category")?.name||"اخبار",date:new Intl.DateTimeFormat("fa-IR",{dateStyle:"long"}).format(new Date(post.date)),publishedAt:post.date,modifiedAt:post.modified,image:preferEditorial?fallback:await downloadImage(media?.source_url,post.slug,fallback),imageAlt:media?.alt_text||title,source:author,lead:excerpt,body:[],takeaways:[],contentHtml:await localizeContentImages(post.content?.rendered||"",post.slug,fallback),wordpressId:post.id,originalUrl:post.link})}
  await fs.mkdir(path.dirname(target),{recursive:true});await fs.writeFile(target,JSON.stringify({syncedAt:new Date().toISOString(),api,posts:result},null,2));console.log(`Synced ${result.length} published WordPress posts.`)
}catch(error){console.error(`WordPress sync failed: ${error.message}`);if(strict)process.exit(1);console.log("Existing snapshot preserved; build can continue with cached or fallback content.")}
